### install cypress

```
npm i cypress
```

### Execute Cypress

```
npx cypress open
```

### Run Cypress in CLI

```
npx cypress run
```

#### Execute only the test in group

```
it.only("",  () => {})
```

#### Skip the test in group

```
it.skip("",  () => {})
```
